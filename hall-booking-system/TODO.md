# TODO - About page styling alignment

- [ ] Inspect current `about.html` and `about.css` structure/classes.
- [x] Update `about.html` to add missing class hooks (`.navbar`, `.nav-container`, `.logo`, `.nav-links`) while keeping the member cards and introduction sections.

- [x] Update `about.css` to match the theme used in `homepage.css` / `history.css` (dark navy + gold accents, card styles, responsive).

- [x] Verify no changes are made to `homepage.html` or `history.html`.

- [ ] Quick smoke test: open `about.html` in browser and visually confirm nav/hero/cards look consistent.

